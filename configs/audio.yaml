audio:
  sample_rate: 24000
  channels: 1
  export_sample_rate: 44100
  export_channels: 2
  format: wav

separation:
  engine: demucs
  stem: vocals

alignment:
  tolerance_ratio: 0.1
  max_stretch_ratio: 1.2
  min_stretch_ratio: 0.8
